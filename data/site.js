// Site-wide switches.
//
// comingSoon: when true, the homepage shows ONLY the hero + a "we're
// cooking" panel, and the nav hides Projects / Contact / Link tree.
// The Research section stays fully live either way.
//
// When the full site is ready, flip this to false — everything
// (About, Projects, Services, Experience, Contact, Link tree) comes
// back instantly. Nothing was deleted.

export const siteConfig = {
  comingSoon: true,
}

export default siteConfig
